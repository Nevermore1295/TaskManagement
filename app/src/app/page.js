export default function Home() {
  return (
    <div>
      <h1>Welcome to My Next.js App with App Router</h1>
      <p>API Routes are still located in the `pages/api` directory.</p>
      <p>Visit <a href="/api/test">/api/test</a> to test the API.</p>
    </div>
  );
}

